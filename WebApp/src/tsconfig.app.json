{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": []
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ],
    "angularCompilerOptions": {
      "enableIvy": false    // do not enable Ivymode, it may cause to fail to build in product mode because Ivymode now just beta version
    }
}
