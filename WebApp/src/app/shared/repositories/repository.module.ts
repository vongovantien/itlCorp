import { NgModule } from '@angular/core';
@NgModule({
    providers: [
        // SystemRepo,
        // AccountingRepo,
        // OperationRepo,
        // CatalogueRepo,
        // DocumentationRepo,
        // ExportRepo,
        // SettingRepo,
        // API_MENU
    ],
})
export class RepositoryModule {
}
